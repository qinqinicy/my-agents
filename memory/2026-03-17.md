# 2026-03-17

## 今日完成

### Java 后端项目调试
- **问题**: 决策系统 `/user/getUserById` API 返回 401 "用户授权过期"
- **根因分析**:
  - JAR 包编译 Java 版本不匹配（本地 Java 17，服务器 Java 8）
  - Maven 仓库配置使用 HTTP 被新版 Maven 阻止
  - `GetToken.java` 包含无效导入 `sun.java2d.pipe`
  - 前端 token 获取时机问题（getUserInfo 在 token 获取前调用）
- **修复内容**:
  - 修改 `pom.xml`: 编译目标从 17 改为 8，仓库 URL 改为 HTTPS
  - 修复 `GetToken.java`: 删除无效导入
  - 修复 `JwtToken.java`: 添加多字段名映射（userId/sub/userid, userName/name/username）
  - 本地 Java 环境配置完成（OpenJDK 17，Maven 3.9）
  - 成功编译并打包 JAR（63.3 MB，Java 8 兼容字节码）
- **待验证**: 服务器部署后测试 token 传递流程

### 前端 token 问题排查
- 分析 `App.vue` token 获取逻辑
- 发现 `getUserInfo()` 在脚本加载时立即执行，可能早于 token 获取
- 建议延迟调用或等待 token 获取后再请求

### 技能开发
- 新增 `tavily-search` 技能：AI 优化搜索

## 待办
- [x] 修复 JwtToken.java 多字段名支持
- [x] 重新编译并打包 JAR（包含更新后的 JwtToken.class）
- [ ] 部署新版 JAR 到服务器验证
- [ ] 解决前端 token 传递时序问题

## 记忆维护
- 创建 `memory/topics/工单流转系统.md` 主题文件
- 更新 `04-MEMORY.md` 添加新项目状态

---

*记忆维护完成*

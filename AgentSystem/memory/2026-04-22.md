# 2026-04-22 日志

## 今日进展

### ZLMediaKit + StreamUI 视频平台调试

完成视频平台关键问题修复和配置：

**核心问题修复**：
- Docker 网络模式：从 bridge 改为 `--network host`，解决"wait http response header timeout"
- API 参数修正：添加 `vhost=__defaultVhost__` 参数解决"Required parameter missed: vhost"
- HLS 录制路径：`hls_demand=0`（连续录制）+ `hls_save_path=/opt/media/www`
- 录像目录打通：创建 symlink `/opt/media/bin/www` → `/opt/media/www`，解决 StreamUI 录像路径 404

**关键配置**：
- ZLM Docker：`docker run --network host -v /opt/media/conf:/opt/media/conf -v /opt/media/www:/opt/media/www zlmediakit/zlmediakit:master`
- ZLM API 添加拉流：`/index/api/addStreamProxy`，启用 `enable_hls=1` 和 `enable_mp4=1`
- nginx 录像路径：`location /record/ { alias /opt/media/www/record/; autoindex on; }`

**验证通过**：
- VLC 直接拉流：正常播放
- StreamUI 播放：正常
- 录像录制：HLS + MP4 双模式正常

### 生成视频平台运维手册

- HTML 版本：`myagents_files/视频平台运维手册.html`
- PDF 版本：`myagents_files/视频平台运维手册.pdf`（505KB）
- 包含所有密码（StreamUI: streamui/streamui，ZLM: mysecret123）
- 包含 MinIO 集群信息待填写

### MinIO 集成规划

- MinIO 集群：`10.129.80.211:9001`，Access: `minioadmin`，Secret: `Ka93@fnnbd2A`
- 待安装 rclone 进行录像文件同步（服务器离线，需手动下载）
- rclone 下载地址：`https://downloads.rclone.org/rclone-current-linux-amd64.zip` 或 `https://downloads.rclone.org/1.68.2/rclone-v1.68.2-linux-amd64.zip`

## 遇到的问题

- 录像回看只显示一条记录 → 实际多条录制存在，界面展示问题（不阻塞）
- rclone 需在服务器离线环境下手动下载安装

## 明日待办

- [ ] 服务器安装 rclone 并配置 MinIO 同步
- [ ] 确认录像自动归档到 MinIO 的配置

## 记忆更新触发

触发记忆更新，记录视频平台调试经验到横琴项目主题记忆。

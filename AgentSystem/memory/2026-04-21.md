# 2026-04-21 日志

## 今日进展

### 横琴全空间无人体系智能数据中心
- 完成 IoTDB 2.0.7 集群接入（10.129.80.142/201/230）
  - 启用 REST API (port 18080) 和 MQTT 服务 (port 1883)
  - 创建存储组 root.cszl
  - 创建 31 个时间序列字段（城市治理无人机数据）
  - MQTT + REST 全流程验证通过
  - 关键发现：IoTDB 2.0.7 内置 MQTT 需要特定 JSON 格式（device/measurements/values 数组格式），普通 flat JSON 不兼容
- 生成 IoTDB 运维手册（PDF + HTML），保存到 `Projects/work/[WIP] 横琴全空间无人体系智能数据中心项目/docs/IoTDB运维手册/`

### 项目整理
- 清理冗余目录 `Projects/work/横琴全空间无人体系智能数据中心/`（已合并到主项目）
- 更新横琴项目记忆档案

## 遇到的问题

- IoTDB 2.0.7 内置 MQTT 不接受普通 flat JSON 格式，需要转换为 `{device, measurements[], values[]}` 格式
- `time` 是保留字，模板中需用反引号包裹
- INT 类型不支持，需用 INT32

## 明日待办

- [ ] 接入运营中心场景 (root.yyzx)
- [ ] 接入智慧灯杆场景 (root.zhdg)

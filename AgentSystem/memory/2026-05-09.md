# 2026-05-09 日志

## 记忆维护

### 更新内容
- **视频平台运维 topic**：全面更新，回归 ZLM + StreamUI 模式
  - 流命名：machinenest、drone、5gacamera
  - 录制路径：`/opt/media/www/record/{场景}/{流ID}/`
  - rclone sync 改为 sync_by_scene.sh 按场景分流
  - MinIO IAM：cszlkey 只读访问 cszl-media
  - S3 API + rclone serve HTTP 对外接口
  - yyzx 场景已清理
  - Oryx 方案搁置（8080 端口 errno=98）

- **04-MEMORY.md**：
  - 新增：流命名规范化、rclone sync 按场景分流、MinIO IAM
  - 更新：视频平台状态为 ZLM + StreamUI
  - 更新：Recent Work 为流命名规范化、MinIO IAM、IoTDB 场景接入

- **视频平台运维手册**（v2.1）：
  - 新增 6.5 MinIO IAM 权限管理
  - 新增 6.6 对外接口服务（S3 API、rclone serve HTTP、Console）
  - 清理 yyzx 场景相关配置

### 当前系统状态

| 组件 | 说明 |
|------|------|
| ZLM + StreamUI | 主用流媒体 |
| MinIO | cszl-media 桶 |
| rclone sync | sync_by_scene.sh，每5分钟执行 |
| S3 API | 8091 端口 Basic Auth |
